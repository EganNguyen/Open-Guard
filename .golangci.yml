linters:
  enable:
    - errcheck
    - gosec
    - staticcheck
    - unparam
    - govet
    - unused
    - misspell
    - revive

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
