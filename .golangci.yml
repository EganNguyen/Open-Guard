run:
  timeout: 10m
  issues-exit-code: 1
  tests: true

linters:
  enable:
    - errcheck
    - staticcheck
    - unparam
    - govet
    - unused
    - revive

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true

issues:
  exclude-dirs:
    - examples
