{
  "name": "open-guard",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack security guard library with React dashboard",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}